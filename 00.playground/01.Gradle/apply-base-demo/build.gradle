plugins {
	id "base"
}

description = "A trivial Gradle build"
version = "1.0"

task zip(type: Zip, group: "Archive", description: "Archives sources in a zip file") {
	from "src"
	archiveFileName = "base-demo-1.0.zip"
}
